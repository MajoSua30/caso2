@model IEnumerable<caso2.Models.Lista>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombreactvidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fechahacer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombreactvidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fechahacer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.Nombrelista">Edit</a> |
                    <a asp-action="Details" class="btn btn-pimary" asp-route-id="@item.Nombrelista">Details</a> |
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.Nombrelista">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
